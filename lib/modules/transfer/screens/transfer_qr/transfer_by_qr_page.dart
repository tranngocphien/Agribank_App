import 'package:flutter/material.dart';

class TransferByQrPage extends StatelessWidget {
  const TransferByQrPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
